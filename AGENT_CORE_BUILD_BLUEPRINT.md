# Agent Core 구축 설계도면

## 문서 역할과 사용 방법

- 목적: Agent Core 최초 구축의 목적, 근거, 고정된 결정, 단계 순서와 검증 기준을 보존한다.
- 읽는 시점: 최초 구축 의도와 완료된 Stage의 설계 기준을 역사적으로 확인할 때.
- 책임: 사용자가 단계 구성과 성공 조건을 승인하고 에이전트가 문구를 유지한다.
- 상태: 완료 설계. 28개 Stage 종료 후 현재 활성 설계가 아니며 현재 상태는 `STATE.md`가 소유한다.
- 관련 권위: 최신 사용자 지시가 이 문서보다 우선한다.
- 문서 역할: 완료된 최초 구축의 설계 기준이다. 현재 첫 행동이나 진행 상태를 소유하지 않는다.
- 대상 프로젝트: 이 문서가 있는 독립 `Agent Core` 저장소.
- 참고 구현: `D:\AI Agent\creator`의 기존 Core. 별도 사용자 지시가 없는 한 읽기 전용 참고 자료로만 사용한다.
- 권위: 최신 사용자 지시가 이 문서보다 우선한다. 이 문서는 승인된 전체 방향과 단계별 성공 조건을 소유하지만 개별 단계의 구체적인 파일 변경과 구현 방법을 미리 승인하지 않는다.
- 현재 상태·현재 단계·첫 다음 행동: [STATE.md](STATE.md)가 소유한다. 이 문서는 복제하지 않는다.

새 세션은 작업 전에 이 문서를 처음부터 끝까지 읽는다. 이전 채팅, 과거 Assistant의 기억 또는 참고 구현의 기존 결론을 권위로 가정하지 않는다. 현재 단계보다 뒤의 구현을 선행하지 않으며, 각 단계의 성공 게이트가 증거와 함께 통과된 후에만 다음 단계로 전환한다.

Stage 4에서 정식 상태 owner가 [STATE.md](STATE.md)로 확정되었다. 이 문서는 안정된 전체 설계와 단계 지도만 소유하며 현재 상태를 복제하지 않는다. 인수인계 계약의 전체 설계는 Stage 10이 소유한다.

## 확인된 기준 상태

아래 내용은 2026-08-13에 `D:\AI Agent\creator`의 파일, Git 상태와 로컬 검증을 직접 확인한 결과다. 새 세션은 이를 참고 기준으로 사용할 수 있지만, 동적인 상태가 설계 판단에 중요하면 해당 시점에 다시 측정한다.

- 기존 Core는 `rules`, `docs`, `schemas`, `src/file_data`, `tests`, `failures`로 구성되어 있다.
- 기존 Core에는 규칙 11개, 계약 문서 9개, JSON Schema 11개, 해결 실패 사례 6개가 있다.
- `core/src/file_data`에는 record 저장, 작업 상태, 출처·지식·결정, 수명주기, 선택적 컨텍스트, 문서 파생 데이터와 유지보수 검증 구현이 있다.
- Python 3.12.13에서 Core 테스트 144개가 모두 통과했다.
- 유지보수 검증은 문서 61개, 링크 72개, Python 파일 32개, Schema 15개를 검사해 오류 0건으로 통과했다.
- 실제 프로젝트의 구조화 runtime data는 source, knowledge, decision, lifecycle state, work state와 event 모두 0건이었다.
- 실제 저장소에서 `RecordStore` 쓰기는 의도적으로 차단되고 임시 테스트 root에서만 허용된다.
- 현재 실제 사용의 중심은 규칙 라우팅, 문서 정본, 안전 경계, 검증과 Markdown 기반 컨텍스트이며 구조화 작업·지식 Runtime은 구현량에 비해 실사용 증거가 부족하다.
- 기존 구현에는 `extension/data`, `PROJECT_RULES.md`, `SESSION_HANDOFF.md`, `extension/src`, `extension/tests`, 특정 root 이름과 첫 프로젝트 패키지명에 대한 결합이 남아 있다.

위 사실에서 도출한 설계 판단은 다음과 같다.

- 기존 Core는 폐기할 초안이 아니라 검증된 참고 구현이다.
- 기존 Core 전체가 1차 Core의 필수 범위라는 뜻은 아니다.
- 실제 사용 근거가 있는 Agent 운영 Kernel과 실험적인 Agent Data Platform을 먼저 분리해야 한다.
- 테스트 통과는 구현 계약의 정확성을 증명하지만 실제 운영 필요성과 효용까지 증명하지는 않는다.

## 작업 의도

이 작업은 특정 프로젝트나 특정 AI Agent에 종속되지 않는 공통 Agent 운영 기반을 구축하기 위해 진행한다.

현재 `creator` 프로젝트에는 Agent의 작업 규칙, 권한과 승인 경계, 문서와 정보의 소유 구조, 작업 상태와 인수인계, 검증, 실패 지식, 선택적 컨텍스트, 파일 기반 데이터 Runtime 등이 상당 부분 구현되어 있다. 그러나 실제로 사용 중인 핵심 기능과 실험적으로 확장된 기능이 함께 섞여 있고, 첫 번째 도메인 프로젝트의 구조와 경로에 결합된 부분도 남아 있다.

따라서 기존 Core를 그대로 복사하거나 처음부터 완성된 구조를 한 번에 결정하지 않는다. 실제 구현과 사용 상태를 근거로 Agent Core에 필요한 요소를 다시 판정하고, Core 자체가 독립적인 프로젝트로 작동할 수 있도록 순서대로 구축한다.

최종적으로는 서로 다른 Agent가 대화 기억에 의존하지 않고 같은 규칙·상태·근거를 사용하며, 사용자가 같은 설명과 지시를 반복하지 않아도 일정한 품질로 작업을 시작하고 이어받고 검증할 수 있는 기반을 만드는 것을 목표로 한다.

## 요청사항

- Host 연결과 배포 방식보다 독립 Agent Core 자체의 구축을 먼저 진행한다.
- 기존 `creator`의 Core를 실제 구현 근거로 사용하되 정답이나 그대로 옮길 대상으로 간주하지 않는다.
- 기존 구현에서 실제로 사용되는 핵심, 검증됐지만 사용 근거가 부족한 기능, 실험 기능을 구분한다.
- Agent 공통 운영에 필요한 요소와 특정 프로젝트·도메인에 속하는 요소를 분리한다.
- 처음부터 최종 정답을 확정하지 않고 각 요소를 선행 조건과 검증 결과에 따라 순차적으로 구축한다.
- 세부 구현 방법은 해당 요소의 구축 단계가 되었을 때 별도로 설계한다.
- 문서, 코드, 테스트와 실제 사용 상태를 함께 확인하고 검증되지 않은 주장을 완료 근거로 사용하지 않는다.
- 이미 구현됐다는 이유만으로 사용되지 않는 데이터 Runtime이나 자동화를 1차 Core 필수 범위에 포함하지 않는다.
- Core가 자기 자신의 작업 규칙, 상태, 변경과 실패를 관리할 수 있는지를 우선 검증한다.
- Host, Extension 일반화, submodule과 배포 브랜치 문제는 독립 Core의 1차 완료 조건을 충족한 이후에 검토한다.

## 확정된 설계 결정

다음 항목은 이후 세션이 임의로 되돌리거나 다른 전제로 바꾸지 않는다. 변경이 필요하면 근거와 영향을 보고하고 사용자 결정을 먼저 받는다.

1. 독립 Agent Core 자체를 먼저 구축한다.
2. Host 연결, submodule, 배포 브랜치와 Extension 일반화는 Stage 28 이전에 구현하지 않는다.
3. 기존 `creator` Core는 읽기 전용 근거이자 후보 구현이며 복사 대상이나 최종 정답이 아니다.
4. 1차 Core의 중심은 Agent 운영 Kernel이다.
5. 구조화 작업 상태·지식·수명주기 Runtime은 실제 효용 검증 전까지 필수 Kernel로 간주하지 않는다.
6. 문서, 코드, 테스트와 실제 사용 증거를 분리해 평가한다.
7. 각 단계는 세부 설계, 구현, 검증, 성공 게이트 판정 순서로 진행한다.
8. 한 단계가 통과되지 않은 상태에서 후속 단계 구현으로 문제를 우회하지 않는다.
9. 합성 fixture, 단위 테스트와 실제 자체 운영 검증을 서로 다른 증거 수준으로 기록한다.
10. Core가 자기 자신을 운영하고 다른 Agent가 이어받을 수 있어야 독립 Core 1차 완료로 판정한다.

## 아직 확정하지 않는 사항

다음 항목은 현재 설계에서 의도적으로 열어 둔다. 해당 단계의 증거 없이 미리 선택하지 않는다.

- 최종 디렉터리와 파일 이름
- 지원할 Agent의 정확한 범위와 Agent별 진입 파일 형식
- 규칙 문서의 최종 개수와 분할 방식
- 구현 언어, 패키지명과 필수 runtime 버전
- CLI 제공 범위와 공개 Python API
- 구조화 work state Runtime의 채택 여부
- source·knowledge·decision·lifecycle Runtime의 채택 여부
- Host 매니페스트, Core 배포 방식과 Git 연결 구조
- `main`·배포 브랜치 분리 여부

## 전체 구축 단계와 의존 관계

28개 항목은 독립된 체크리스트가 아니라 아래 순서로 연결된 단계다.

### Phase A — 목적과 경계 확정

`1 → 2 → 3 → 4`

- Core의 목적을 확정하고, 필요한 범위와 내부 책임·정보 정본을 결정한다.
- 이 단계가 끝나기 전에는 기존 Core 파일을 이식하거나 새 Runtime을 구현하지 않는다.

### Phase B — Agent 운영 Kernel 구축

`5 → 6 → 7 → 8 → 9 → 10 → 11 → 12 → 13`

- Agent 진입, 정책, 규칙 라우팅, 작업 계약, 단계 설계, 인수인계와 변경 통제를 구축한다.
- 각 요소는 앞 단계에서 확정된 정본·경계 원칙을 따라야 한다.

### Phase C — 검증 가능한 실행 기반 구축

`14 → 15 → 16 → 17 → 18 → 19`

- 검증 수준, 무결성 검사, 파생 artifact, 실패 지식, 컨텍스트와 최소 도구를 구축한다.
- 도구는 앞서 문서로 확정된 계약을 강제하는 최소 범위만 구현한다.

### Phase D — Core 자체 운영 입증

`20 → 21 → 22`

- 실제 Core 변경, 다른 Agent의 재개, 통합 검증을 end-to-end로 입증한다.
- 단위 테스트만으로 이 Phase를 통과할 수 없다.

### Phase E — 실험 기능 판정

`23 → 24 → 25`

- 실험 기능을 Kernel에서 격리하고 작업 상태 및 지식 Runtime의 효용을 각각 평가한다.
- Stage 24와 25의 성공은 반드시 Runtime 채택을 뜻하지 않는다. 증거에 따른 `채택`, `실험 유지`, `제외` 결정 자체가 이 단계의 결과다.

### Phase F — 독립 Core 완료와 다음 단계 경계

`26 → 27 → 28`

- 버전·호환성을 확정하고 독립 Core의 완료를 판정한 다음 Host 설계의 진입 조건만 정의한다.
- Stage 28은 Host 연결 구현 단계가 아니다.

## 단계별 실행 절차

각 Stage는 아래 절차로 진행한다. 구현 세션은 이 순서를 생략하거나 여러 Stage를 한꺼번에 완료 처리하지 않는다.

1. **진입 확인**: 이전 Stage의 성공 게이트와 증거를 읽고 현재 Stage의 선행 조건이 충족됐는지 확인한다.
2. **현황 조사**: 현재 저장소와 필요한 범위의 참고 구현을 읽기 전용으로 조사한다. 과거 보고서보다 현재 파일과 실행 결과를 우선한다.
3. **단계 설계**: 해당 Stage에서 결정할 내용, 변경 대상, 제외 범위, 세부 구현안, 검증 명령, 복구 방법을 제안한다.
4. **사용자 확인**: 선택에 따라 결과가 materially 달라지거나 정책·구조·Core 범위가 바뀌는 경우 구현 전에 사용자 결정을 받는다.
5. **구현**: 승인되거나 명백히 위임된 Stage 범위만 최소 단위로 변경한다. 참고 구현의 이식과 개선은 검토 가능한 단위로 분리한다.
6. **직접 검증**: 해당 Stage의 `검증 방법`을 실제로 실행하고 미실행 항목을 통과로 표시하지 않는다.
7. **성공 게이트 판정**: 각 성공 조건을 `pass`, `fail`, `not_run`, `not_applicable` 중 하나로 판정하고 근거를 연결한다.
8. **상태 갱신**: 통과한 결정, 남은 위험, 변경 파일, 검증 결과와 다음 Stage의 첫 행동을 현재 상태 owner에 기록한다.
9. **전환**: 모든 필수 게이트가 `pass`인 경우에만 다음 Stage를 활성화한다. 실패하면 현재 Stage에 머물러 원인과 재시작 조건을 기록한다.

## 단계 산출물과 증거 계약

각 Stage는 최소한 다음 결과를 남겨야 한다. 별도 파일을 무조건 만들라는 의미는 아니며, Stage 4에서 정한 가장 좁은 정본에 보존한다.

- Stage ID와 목적
- 진입 시 확인한 선행 게이트
- 확정한 결정과 그 근거
- 검토했지만 채택하지 않은 주요 대안과 이유
- 변경한 정확한 파일 범위
- 실행한 검증과 결과
- 성공 게이트별 판정
- 알려진 한계와 미검증 항목
- 다음 Stage 또는 재시작의 첫 행동

증거는 다음 수준을 구분한다.

| 증거 수준 | 의미 |
|---|---|
| `documented` | 문서에 계약이나 판단이 기록됨 |
| `static-validated` | 링크, schema, AST, 구조 검사 통과 |
| `unit-tested` | 격리된 자동 테스트 통과 |
| `integrated` | 여러 구성요소를 연결한 통합 검증 통과 |
| `self-operated` | 실제 Core 작업에서 처음부터 끝까지 사용됨 |
| `cross-agent` | 다른 Agent가 대화 없이 재개함 |
| `user-confirmed` | 사용자가 결과나 판단을 확인함 |

상위 수준을 달성하지 않았으면 하위 수준의 성공을 확대해서 보고하지 않는다.

## 변경·중단·복구 원칙

- `D:\AI Agent\creator`는 별도 승인 없이 수정하지 않는다.
- 현재 Agent Core 저장소의 기존 사용자 변경과 관련 없는 파일을 보존한다.
- 이 문서를 읽었다는 사실만으로 삭제, 이동, 설치, 외부 쓰기, push 또는 광범위한 Git 변경이 승인되지는 않는다.
- 구현 중 사용자 교정이 현재 Stage의 목표·범위·게이트를 바꾸면 대기 중 변경을 중단하고 기존 단계 설계를 무효화한다.
- 같은 목표가 세 번 연속 실패하면 같은 방법의 반복을 중단하고 실패 유형, 확인한 원인, 대체 방법과 재시작 조건을 기록한다.
- 도구 성공 응답만으로 파일 변경을 완료 처리하지 않는다. 실제 파일, diff와 필요한 parser·test 결과를 다시 확인한다.
- 검증이 기존 결함 때문에 실패하면 새 결함과 기준 상태 결함을 구분하고, 이를 숨기기 위해 검증 범위를 축소하지 않는다.
- Stage 범위를 넘어야만 해결되는 문제가 발견되면 후속 Stage를 선행 구현하지 말고 현재 blocker와 필요한 결정으로 보고한다.

## 새 세션 시작 체크리스트

새 구현 세션은 다음을 수행한 뒤 작업을 시작한다.

1. 이 문서를 EOF까지 읽는다.
2. 최신 사용자 지시와 이 문서의 충돌 여부를 확인한다.
3. 현재 상태 owner와 현재 Stage를 확인한다.
4. 이전 Stage의 성공 게이트와 증거를 확인한다.
5. Git root, branch, status와 기존 변경을 읽기 전용으로 확인한다.
6. 현재 Stage에 필요한 참고 구현의 정확한 범위만 읽는다.
7. 현재 Stage의 목표, 포함 범위, 제외 범위, 성공 게이트와 첫 행동을 짧게 재진술한다.
8. 세부 구현 설계를 사용자에게 제안한 뒤 필요한 승인을 받고 실행한다.

## 구축 항목

## 1. Agent Core의 목적과 최종 책임

### 구현 목표

Agent Core가 해결해야 하는 문제와 최종적으로 보장해야 하는 결과를 하나의 기준으로 확정한다.

### 구현할 내용

- Core의 목적, 주요 사용자, 해결 대상 문제를 정의한다.
- Core가 보장할 결과와 보장하지 않을 결과를 구분한다.
- Agent 교체 가능성, 반복 지시 감소, 작업 품질의 일관성, 재개 가능성을 최상위 성과로 정의한다.
- 이후 모든 구성요소의 포함 여부를 판단할 공통 기준을 만든다.

### 검증 방법

- 기존 `creator` 구현과 인수인계 문서의 요구를 목적 정의에 대조한다.
- 서로 충돌하거나 측정할 수 없는 표현을 찾는다.
- 대표 작업을 목적 정의에 대입해 Core 책임인지 아닌지 판정한다.

### 성공 게이트

- Core의 목적을 한 문단으로 설명할 수 있다.
- 핵심 보장과 비보장 항목이 명시되어 있다.
- 이후 기능의 포함 여부를 같은 기준으로 판정할 수 있다.

## 2. Core Kernel의 포함 범위와 제외 범위

### 구현 목표

1차 Core에 반드시 필요한 최소 기능과 이후로 미룰 기능을 분리한다.

### 구현할 내용

- 기능을 `필수 Kernel`, `검증된 선택 기능`, `실험 기능`, `제외 기능`으로 분류한다.
- 각 기능의 포함 근거, 실제 사용자, 선행 의존성, 제거 영향을 기록한다.
- Host·도메인·외부 도구 기능이 Kernel에 섞이지 않게 한다.
- 실제 사용 증거가 없는 데이터 Runtime은 기본 활성 범위에서 제외한다.

### 검증 방법

- 현재 Core의 규칙, 문서, 코드, 테스트, 실제 호출 경로를 분류표와 대조한다.
- 필수 Kernel만 남겼을 때 독립 Core의 핵심 작업 흐름이 가능한지 확인한다.
- 제외하거나 보류한 기능이 필수 기능의 숨은 의존성인지 검사한다.

### 성공 게이트

- 모든 기존 기능이 하나의 범주로 분류되어 있다.
- 필수 Kernel에 Host·도메인 전용 기능이 없다.
- 보류 기능 없이도 1차 Core의 목표를 검증할 수 있다.

## 3. Core 내부 계층과 구성요소 경계

### 구현 목표

Core 내부 책임과 의존 방향을 명확하게 분리한다.

### 구현할 내용

- 진입·정책, 작업 계약, 정보·문서, 검증 도구, 실패 지식, 실험 Runtime 계층을 정의한다.
- 각 계층의 소유 정보, 공개 인터페이스, 금지 의존 방향을 정한다.
- 상위 정책이 하위 구현에 종속되지 않게 한다.
- 실험 Runtime이 Kernel의 시작과 기본 검증을 막지 않게 격리한다.

### 검증 방법

- 문서 링크, Python import, CLI 호출과 파일 경로를 이용해 실제 의존 그래프를 검사한다.
- 역방향 의존, 순환 의존, 중복 인터페이스를 탐지한다.
- 한 계층을 제거했을 때 영향을 받는 범위를 확인한다.

### 성공 게이트

- 모든 구성요소에 단일 계층과 책임이 배정되어 있다.
- 의존 방향이 한 방향이며 순환하지 않는다.
- 실험 계층을 제외해도 Kernel이 시작되고 검증된다.

## 4. 정보 유형과 단일 정본 소유 구조

### 구현 목표

Agent가 같은 사실의 여러 사본 중 무엇을 믿어야 하는지 혼동하지 않게 한다.

### 구현할 내용

- 정책, 조건부 규칙, 계약, 현재 상태, 작업 설계, 결정, 실패 지식, 파생 artifact, 완료 이력을 구분한다.
- 각 정보 유형의 단일 정본 위치와 갱신 책임을 정한다.
- 다른 문서는 정본을 복제하지 않고 링크하거나 결정론적으로 파생하게 한다.
- 현재 상태와 완료 이력을 분리한다.

### 검증 방법

- 동일한 활성 사실이 두 문서 이상에 존재하는지 검사한다.
- 각 문서가 소유할 정보와 소유하지 않을 정보를 준수하는지 점검한다.
- 정본 변경 후 참조와 파생물이 올바르게 갱신되는지 확인한다.

### 성공 게이트

- 모든 활성 정보에 정확히 하나의 정본이 있다.
- 상태·정책·완료 이력이 서로 복제되지 않는다.
- Agent가 정본을 경로와 책임으로 판별할 수 있다.

## 5. 독립 Core 프로젝트의 Agent 진입 구조

### 구현 목표

서로 다른 Agent가 독립 Core 프로젝트를 열었을 때 같은 정책과 현재 상태로 합류하게 한다.

### 구현할 내용

- Agent별 최소 진입 파일을 둔다.
- 진입 파일은 정책을 복제하지 않고 공통 정책 정본을 가리키게 한다.
- 공통 정책에서 현재 상태와 조건부 규칙을 선택하게 한다.
- 사람이 읽는 시작 문서와 Agent가 읽는 시작 문서의 책임을 구분한다.

### 검증 방법

- 지원 대상 Agent별 새 세션에서 실제 읽기 순서를 확인한다.
- 진입 파일 간 정책 내용 차이와 중복을 검사한다.
- 사용자 추가 설명 없이 같은 현재 상태와 정책에 도달하는지 확인한다.

### 성공 게이트

- 모든 지원 Agent가 동일한 정책 정본과 현재 상태에 도달한다.
- Agent별 진입 파일에는 독자적인 정책이나 상태가 없다.
- 새 세션이 수동 안내 없이 올바른 시작 경로를 찾는다.

## 6. 공통 권한·승인·안전 정책

### 구현 목표

Agent 종류와 작업 종류가 달라도 동일한 권한·안전 경계를 적용한다.

### 구현할 내용

- 사용자 지시, Core 정책, 작업별 owner의 권위 순서를 정의한다.
- 삭제, 이동, 외부 쓰기, 비용 발생, 설치, 게시, 권한 변경의 승인 경계를 정의한다.
- 최소 권한, 범위 제한, 사용자 변경 보존, 증거 구분 원칙을 포함한다.
- 반복 실패와 권한 부족 시 중단·복구 규칙을 정의한다.

### 검증 방법

- 승인 필요·불필요·금지 작업 시나리오를 정책에 대입한다.
- 서로 다른 Agent가 같은 시나리오에 같은 결론을 내리는지 확인한다.
- 프롬프트 지시만으로 실제 권한 경계를 대신하는 부분이 없는지 점검한다.

### 성공 게이트

- 주요 위험 작업의 승인 여부가 일관되게 판정된다.
- 정책 충돌 시 적용할 우선순위가 명확하다.
- 권한이 없는 작업은 변경 전에 중단된다.

## 7. 작업 등급 분류와 조건부 규칙 라우팅

### 구현 목표

모든 규칙을 미리 읽지 않고 현재 행동에 필요한 최소 규칙만 정확히 선택한다.

### 구현할 내용

- `quick`, `standard`, `controlled` 작업 등급과 판정 조건을 정의한다.
- 조건부 규칙의 observable trigger와 단일 라우터를 구축한다.
- 작업 도중 행동 경계가 바뀌면 규칙을 다시 선택하게 한다.
- 초기·구어체·중간 전환·유사하지만 비해당인 요청 fixture를 만든다.

### 검증 방법

- 라우팅 fixture를 결정적으로 검사한다.
- 지원 Agent의 새 세션에서 의미 기반 라우팅을 재현한다.
- 예상 규칙과 금지 규칙, 중복 route, 고아 규칙을 확인한다.

### 성공 게이트

- 모든 활성 규칙에 정확히 하나의 route가 있다.
- 대표 요청에서 필요한 규칙만 선택된다.
- 작업 도중 새 경계가 생기면 추가 규칙이 누락 없이 선택된다.

## 8. 작업 요청·범위·완료 조건 계약

### 구현 목표

Agent가 무엇을 해야 하고 하지 말아야 하며 무엇으로 완료를 판단할지 명시적으로 유지한다.

### 구현할 내용

- 원하는 결과, 허용 행동, 제외 범위, 입력 참조, 보호 경계, 필요한 결정, 검증 수준을 정의한다.
- 작업 시작 전에 최소 계약을 확인하는 절차를 만든다.
- 사용자 교정으로 결과나 범위가 바뀌면 기존 계약을 무효화한다.
- 완료 보고가 원래 요청과 검증 근거를 추적할 수 있게 한다.

### 검증 방법

- 정상 요청, 모호한 요청, 범위 변경, 사용자 교정 시나리오를 실행한다.
- 제외 범위의 작업이 계획이나 결과에 포함되는지 검사한다.
- 완료 결과를 최초 요청의 각 조건과 대조한다.

### 성공 게이트

- 모든 작업에 검증 가능한 원하는 결과와 제외 범위가 있다.
- 범위 변경 후 오래된 계약으로 실행할 수 없다.
- 완료 판정이 요청 조건과 증거에 직접 연결된다.

## 9. 단계 작업 설계와 계획 무효화 체계

### 구현 목표

여러 단계와 세션에 걸친 작업을 짧고 재개 가능한 설계로 관리한다.

### 구현할 내용

- 전체 설계, 현재 단계 설계, 현재 상태의 책임을 분리한다.
- 단계별 진입, 실행 조각, 종료, 전환 게이트를 정의한다.
- 설계 내용의 fingerprint 또는 동등한 변경 감지 수단을 둔다.
- 사용자 교정이나 범위 변경 시 현재 설계를 무효화하고 재승인하게 한다.

### 검증 방법

- 두 단계 이상의 작업을 중단·재개·전환해 본다.
- 설계 파일을 변경한 뒤 오래된 승인으로 실행이 차단되는지 확인한다.
- 완료 역사와 현재 실행 정보가 설계에 누적되지 않는지 점검한다.

### 성공 게이트

- 한 번에 하나의 단계만 활성 상태다.
- 각 단계에 진입·종료·전환 게이트가 있다.
- 무효화된 설계로는 실행을 계속할 수 없다.

## 10. 현재 상태와 세션 인수인계 체계

### 구현 목표

대화 기록 없이 다른 Agent나 새 세션이 정확한 중단 지점부터 작업을 재개하게 한다.

### 구현할 내용

- 현재 작업, 현재 단계, 확인된 완료, blocker, 승인 상태, 첫 다음 행동을 소유하는 단일 문서를 둔다.
- 완료된 상세와 동적 Git 상태를 시작 문맥에 누적하지 않는다.
- 활성 설계와 필요한 근거만 정확한 경로로 연결한다.
- 같은 workspace와 portable handoff를 구분한다.

### 검증 방법

- 작업 도중 세션을 종료하고 다른 Agent가 상태 문서만으로 재개하게 한다.
- 새 Agent가 목표, 범위, 금지사항, blocker, 첫 행동을 재진술하게 한다.
- 완료된 작업이 시작 문맥에서 제거되는지 확인한다.

### 성공 게이트

- 새 Agent가 대화 기록 없이 첫 다음 행동을 정확히 수행할 수 있다.
- 현재 상태 정본이 하나뿐이다.
- handoff가 실제 복구 가능한 범위를 과장하지 않는다.

## 11. 문서 생성·갱신·종료 수명주기

### 구현 목표

필요한 문서만 만들고 오래된 문서와 중복 정본이 활성 트리에 쌓이지 않게 한다.

### 구현할 내용

- 문서 유형별 생성 조건, 소유자, 독자, 수명, 종료 조건을 정의한다.
- 문서 생성 전 artifact budget과 기존 owner 검색을 요구한다.
- 활성 설계와 상태 문서에 읽기 예산을 적용한다.
- 완료 문서는 정본에 흡수하거나 Git 이력으로 종료한다.

### 검증 방법

- 새 문서 요청을 기존 owner 갱신과 신규 생성으로 판정한다.
- 중복 owner, 고아 문서, 오래된 링크와 읽기 예산 초과를 검사한다.
- 작업 종료 후 임시 문서와 scratch가 남는지 확인한다.

### 성공 게이트

- 모든 유지 문서에 목적, owner, 읽는 조건과 수명이 있다.
- 동일 책임의 활성 문서가 중복되지 않는다.
- 완료 작업이 시작 문맥과 활성 문서 수를 지속적으로 늘리지 않는다.

## 12. 보호 데이터와 외부 효과 통제

### 구현 목표

사용자 원본, 비밀정보, 외부 서비스 변경을 명시적 권한 밖에서 접근하거나 실행하지 않게 한다.

### 구현할 내용

- 보호 경로와 비밀 유형을 정의한다.
- exact 대상과 목적 승인 전 열거·읽기·색인·링크·커밋을 막는다.
- 외부 쓰기와 로컬 분석을 분리한다.
- 보호 데이터가 문서, 로그, 컨텍스트, 테스트 fixture로 복제되지 않게 한다.

### 검증 방법

- 보호 경로의 직접 접근, 재귀 탐색, 링크, Git 추적 시도를 각각 검사한다.
- 외부 변경 요청이 승인 전 단계에서 정지하는지 확인한다.
- 오류 메시지와 보고서가 보호 내용을 노출하지 않는지 점검한다.

### 성공 게이트

- 승인되지 않은 보호 데이터 접근이 실제 읽기 전에 차단된다.
- 보호 데이터가 Git과 재사용 문서에 포함되지 않는다.
- 외부 변경은 exact 승인 없이는 실행되지 않는다.

## 13. Core 자체 변경 통제

### 구현 목표

Core를 기본 불변 영역으로 유지하면서 승인된 최소 변경만 검증해 수용한다.

### 구현할 내용

- Core 변경으로 분류되는 행동과 승인 절차를 정의한다.
- 정확한 이유와 대상 범위가 확인된 후에만 변경하게 한다.
- 무인 작업은 Core 변경을 추론하거나 승인 대기하지 않고 안전하게 중단하게 한다.
- 변경 후 Core 전체와 관련 상위 검증을 실행하게 한다.

### 검증 방법

- 무승인 Core 수정, 승인 범위 밖 수정, 승인된 최소 수정 시나리오를 검사한다.
- Git 상태를 이용해 Core 변경 감지와 범위 대조를 수행한다.
- 무인 작업이 Core를 건드리지 않고 실패 상태를 남기는지 확인한다.

### 성공 게이트

- 승인되지 않은 Core 변경은 완료 상태로 들어갈 수 없다.
- 승인 범위 밖 변경이 자동으로 탐지된다.
- 승인된 변경은 통합 검증을 모두 통과해야 수용된다.

## 14. 검증 수준과 완료 판정 체계

### 구현 목표

생성·구조·도구 실행·사용자 확인을 구분하고 검증 수준을 과장하지 않게 한다.

### 구현할 내용

- 생성, 파싱, 구조 검증, 도구 검증, 실제 사용 검증, 사용자 확인 수준을 정의한다.
- 작업 위험과 결과 유형에 필요한 최소 검증 수준을 연결한다.
- 실행하지 않은 검사를 통과로 기록하지 않게 한다.
- 완료 보고에 수행한 검증과 남은 미검증을 포함한다.

### 검증 방법

- 같은 결과물에 서로 다른 검증 수준을 적용해 판정 차이를 확인한다.
- 도구 미실행, 부분 테스트, 합성 fixture만 통과한 경우를 실패 또는 제한 상태로 분류한다.
- 완료 보고와 실제 검증 로그를 대조한다.

### 성공 게이트

- 모든 완료 결과에 도달한 검증 수준과 증거가 있다.
- 합성 검증과 실제 사용 검증이 구분된다.
- 필수 검증이 빠진 작업은 완료로 판정되지 않는다.

## 15. 문서·링크·Schema·코드 무결성 검사

### 구현 목표

Core의 문서와 실행 계약이 서로 어긋나거나 손상된 상태로 유지되지 않게 한다.

### 구현할 내용

- UTF-8, NUL, 후행 공백, Markdown 링크 검사를 구현한다.
- JSON과 JSON Schema 파싱, Python AST와 관련 테스트를 검사한다.
- 규칙 route의 완전성·유일성과 문서 owner 연결을 검사한다.
- 보호 경로와 금지된 프로젝트·도메인 결합 검사를 포함한다.

### 검증 방법

- 깨진 링크, 잘못된 Schema, Python 문법 오류, 중복 route, 금지 경로를 의도적으로 주입한 fixture를 사용한다.
- 오류별 분류와 비성공 상태가 정확한지 확인한다.
- 정상 checkout에서 전체 검사가 재현 가능하게 통과하는지 확인한다.

### 성공 게이트

- 정의된 모든 결함 fixture가 해당 검사에서 탐지된다.
- 오류를 건너뛰거나 성공으로 축소하지 않는다.
- 정상 Core에서는 오류 0으로 통과한다.

## 16. 결정론적 파생 Artifact 관리

### 구현 목표

사람이 검토하는 정본과 기계가 사용하는 artifact 사이의 drift를 제거한다.

### 구현할 내용

- 각 파생 artifact의 인간 정본, 대상 경로, 재생성 방식과 검증 방식을 정의한다.
- 같은 정본에서 항상 같은 결과가 생성되게 한다.
- 파생 artifact를 직접 수정하지 않고 정본에서 다시 만들게 한다.
- artifact owner와 재생성 대상의 허용 목록을 관리한다.

### 검증 방법

- artifact를 삭제한 뒤 정본에서 재생성한다.
- 같은 입력으로 두 번 생성한 결과를 byte 또는 semantic 기준으로 비교한다.
- artifact만 임의 수정했을 때 drift 검사가 실패하는지 확인한다.

### 성공 게이트

- 모든 유지 artifact에 단일 인간 정본과 재생성 경로가 있다.
- 반복 재생성 결과가 동일하다.
- 정본과 다른 artifact는 통합 검증을 통과하지 못한다.

## 17. 실패 분류와 재사용 가능한 실패 지식

### 구현 목표

현재 blocker와 재사용 가능한 해결 실패를 분리하고 같은 실패의 반복을 줄인다.

### 구현할 내용

- 환경, 권한, 입력, 계약, 구현, 검증 실패 분류를 정의한다.
- 장기 실패 기록의 생성 기준과 단일 owner를 정한다.
- 증상, 조건, 확인된 원인, 실패한 접근, 해결, 검증, 예방 구조를 사용한다.
- 같은 목표의 반복 실패 횟수와 방법 변경 조건을 정의한다.

### 검증 방법

- 일회성 오류, 미해결 blocker, 해결된 범용 실패를 각각 분류한다.
- 필수 절 누락, 미검증 원인, 중복 root cause 문서를 탐지한다.
- 새 작업에서 기존 실패 지식을 검색하고 실제 예방에 사용되는지 확인한다.

### 성공 게이트

- 미해결 실패는 해결된 지식으로 저장되지 않는다.
- 같은 root cause의 중복 실패 문서가 없다.
- 기록된 실패에는 해결과 검증 근거가 모두 있다.

## 18. 선택적 문서 읽기와 컨텍스트 구성

### 구현 목표

전체 프로젝트를 매번 읽지 않고 현재 작업에 필요한 정본만 안전하게 구성한다.

### 구현할 내용

- 명시 문서, 규칙 route, 현재 상태, 검증된 실패의 선택 우선순위를 정의한다.
- 선택 이유, 제외 이유, 문자·byte 크기와 fingerprint를 반환한다.
- 필수 항목과 선택 검색 후보를 구분한다.
- 보호·역사·비활성 자료를 기본 선택에서 제외한다.

### 검증 방법

- 대표 작업별 필수 문서와 금지 문서를 정의해 선택 결과를 평가한다.
- 동일 요청의 반복 결과와 fingerprint를 비교한다.
- 필수 항목 초과, 선택 후보 초과, 보호 경로 입력을 각각 검사한다.

### 성공 게이트

- 대표 작업에서 필수 누락과 금지 포함이 0건이다.
- 같은 입력은 같은 컨텍스트와 fingerprint를 만든다.
- 설정한 크기 예산을 넘을 때 임의 절단하지 않고 명시적으로 실패한다.

## 19. Core 최소 실행 도구와 공개 인터페이스

### 구현 목표

문서 규칙만으로 강제할 수 없는 핵심 검증을 최소한의 안정적인 도구로 제공한다.

### 구현할 내용

- Kernel에 필요한 검사, 상태 조회, artifact 검증, 컨텍스트 구성 인터페이스만 공개한다.
- 라이브러리와 CLI의 책임, 입력, 출력, 오류 종류와 종료 상태를 정의한다.
- 표준 라이브러리를 우선하고 필수 의존성을 최소화한다.
- 실제 소비자가 없는 명령과 호환성 표면은 공개 API에서 제외한다.

### 검증 방법

- 깨끗한 환경에서 bootstrap과 최소 호출을 실행한다.
- 각 공개 인터페이스의 정상·오류·권한 경계 테스트를 수행한다.
- 공개 명령별 실제 사용자 또는 Kernel 소비자와 회귀 테스트 존재 여부를 확인한다.

### 성공 게이트

- 모든 공개 인터페이스에 명확한 소비자와 테스트가 있다.
- 오류가 구조화되고 안정적인 비성공 상태를 반환한다.
- 사용되지 않는 공개 API가 1차 Core에 포함되지 않는다.

## 20. Core 자체 운영 시나리오 검증

### 구현 목표

Core가 자신의 규칙과 도구를 사용해 Core 개발 작업을 안전하게 수행할 수 있음을 증명한다.

### 구현할 내용

- 규칙 변경, 계약 문서 변경, 검증 코드 변경의 대표 시나리오를 정의한다.
- 각 시나리오에서 요청, 권한, 설계, 실행, 검증, 상태 갱신 흐름을 사용한다.
- 실패와 복구 시나리오를 포함한다.
- 결과와 완료 이력을 Git에서 복구할 수 있게 한다.

### 검증 방법

- 실제 독립 Core checkout에서 대표 시나리오를 처음부터 끝까지 수행한다.
- 각 단계에서 적용된 owner, 승인, 변경 범위와 검증 증거를 대조한다.
- 실패를 주입해 중단과 복구 경로를 확인한다.

### 성공 게이트

- 대표 Core 작업이 사용자 반복 설명 없이 완결된다.
- 범위·승인·검증 위반 없이 변경이 완료된다.
- 실패 후 마지막 정상 상태에서 복구할 수 있다.

## 21. 다중 Agent 작업 재개 검증

### 구현 목표

서로 다른 Agent가 같은 파일 기반 상태와 규칙을 사용해 작업을 정확히 이어받게 한다.

### 구현할 내용

- Agent 간 공유할 상태, 설계, 근거와 공유하지 않을 대화 정보를 정의한다.
- 인계 시점의 첫 다음 행동, 승인 상태, 금지사항을 기록한다.
- Agent별 진입점 차이를 공통 정책 뒤에서 흡수한다.
- 동일 workspace와 fresh clone 재개의 조건을 구분한다.

### 검증 방법

- Agent A가 작업 중단 상태를 만들고 Agent B가 채팅 없이 이어받는다.
- Agent B에게 목표, 범위, 결정, blocker와 다음 행동을 재진술하게 한다.
- 다른 Agent 조합과 fresh clone 조건에서 같은 실험을 반복한다.

### 성공 게이트

- Agent B가 추가 설명 없이 올바른 다음 행동을 선택한다.
- 주요 목표·범위·승인 해석이 Agent 간에 달라지지 않는다.
- 실제로 복구할 수 없는 상태를 portable handoff로 표시하지 않는다.

## 22. Core Kernel 통합 검증 게이트

### 구현 목표

Core의 필수 계약을 하나의 재현 가능한 검증 경로로 확인한다.

### 구현할 내용

- runtime preflight, 구조 검사, Core 테스트, 라우팅 검사, 유지보수 검사, 자체 운영 acceptance를 통합한다.
- 단계별 결과와 실패 분류를 구조화해 반환한다.
- 선택 기능 부재와 필수 기능 실패를 구분한다.
- 검증 과정 자체가 Core나 보호 데이터를 변경하지 않게 한다.

### 검증 방법

- 정상 checkout과 각 단계별 오류 fixture에서 통합 게이트를 실행한다.
- 실패 단계, 오류 종류와 다음 복구 owner가 정확한지 확인한다.
- 동일 revision과 환경에서 결과가 반복 재현되는지 확인한다.

### 성공 게이트

- 하나의 명령 또는 동등한 단일 진입점으로 필수 검증이 실행된다.
- 필수 단계 하나라도 실패하면 전체 결과가 성공이 아니다.
- 검증 후 작업 트리와 보호 데이터에 변화가 없다.

## 23. 실험 기능 격리와 승격 기준

### 구현 목표

미래 기능을 보존하면서도 검증되지 않은 복잡성이 Kernel의 필수 경로가 되지 않게 한다.

### 구현할 내용

- 실험 기능의 위치, 상태, owner, 사용자, 종료 조건을 정의한다.
- Kernel 시작·검증·문서 route가 실험 기능에 의존하지 않게 한다.
- 승격에 필요한 실제 사용 횟수, 효과, 비용, 실패·복구 증거를 정의한다.
- 효과가 없는 실험의 종료와 제거 기준을 둔다.

### 검증 방법

- 실험 기능을 비활성화하거나 제거한 상태에서 Kernel 전체 검증을 실행한다.
- 승격 후보의 실제 사용자, 호출 경로, 운영 비용과 개선 효과를 측정한다.
- 실험 기능이 정책이나 시작 문맥을 우회하는지 검사한다.

### 성공 게이트

- 실험 기능 없이 Kernel이 정상 작동한다.
- 모든 실험 기능에 승격 또는 종료 조건이 있다.
- 사용 증거가 없는 기능은 필수 API나 시작 route에 포함되지 않는다.

## 24. 작업 상태 Runtime 필요성 검증

### 구현 목표

작업 event와 snapshot Runtime이 Markdown 상태 관리보다 실제로 나은지 검증한 뒤 채택 여부를 결정한다.

### 구현할 내용

- 복수 세션, 장기 작업, 동시 갱신처럼 Runtime이 필요할 수 있는 조건을 정의한다.
- Markdown handoff 방식과 event replay 방식의 비교 기준을 만든다.
- 실제 Core 작업을 대상으로 제한된 pilot을 수행한다.
- 저장, 복구, 충돌, 운영 비용을 측정한다.

### 검증 방법

- 동일한 작업을 Markdown 방식과 Runtime 방식으로 재개한다.
- 복구 정확도, 상태 drift, Agent 개입 횟수, 파일·명령 비용을 비교한다.
- event append 성공 후 snapshot 실패와 같은 복구 시나리오를 시험한다.

### 성공 게이트

- Runtime이 재개 정확도나 충돌 방지에서 측정 가능한 개선을 제공한다.
- 추가 문서·명령·저장 비용이 개선 효과보다 작다.
- 기준을 충족하지 못하면 1차 Core의 필수 범위로 승격하지 않는다.

## 25. 지식·결정·수명주기 Runtime 필요성 검증

### 구현 목표

구조화 지식과 lifecycle이 Markdown·Git 기반 지식 관리의 한계를 실제로 해결하는지 판정한다.

### 구현할 내용

- 오래된 근거, 충돌 지식, 대체 결정, source drift의 실제 사례를 정의한다.
- Markdown 정본 방식과 구조화 record·lifecycle 방식의 비교 기준을 만든다.
- 자동 승격 없이 제한된 실제 지식 pilot을 수행한다.
- 중복 정본, 현재성 판정, 검색 비용과 사용자 승인 부담을 측정한다.

### 검증 방법

- source 변경, 지식 충돌, 결정 대체, 폐기 시나리오를 양쪽 방식으로 처리한다.
- 현재 지식 선택의 정확성, 복구 가능성, 문서 중복과 운영 비용을 비교한다.
- lifecycle 상태와 원본 record·문서 사이의 drift를 검사한다.

### 성공 게이트

- 구조화 Runtime이 잘못된 오래된 지식 사용을 실제로 줄인다.
- 새로운 중복 정본이나 과도한 승인·유지 비용을 만들지 않는다.
- 기준을 충족하지 못하면 실험 기능으로 유지하거나 제외한다.

## 26. Core 버전·호환성·변경 이력 관리

### 구현 목표

Core의 계약 변화와 실행 환경 요구를 추적하고 안전한 변경·복구 기준을 제공한다.

### 구현할 내용

- Core 버전, 공개 계약 버전, Schema 버전의 관계를 정의한다.
- 호환 변경과 비호환 변경, migration 필요 조건을 구분한다.
- 지원 Python·도구 버전과 선택 의존성을 하나의 정본에서 관리한다.
- 완료 이력은 Git에 두고 현재 호환성 정보만 활성 문서에 유지한다.

### 검증 방법

- 지원 최소·현재 runtime에서 bootstrap과 테스트를 실행한다.
- 이전 계약 fixture를 현재 구현에서 읽고 비호환 변경을 탐지한다.
- 버전 선언, 패키지 설정, 문서와 export 정보가 일치하는지 검사한다.

### 성공 게이트

- 모든 활성 문서와 도구가 같은 지원 버전을 선언한다.
- 비호환 변경은 버전 상승이나 명시적 migration 없이 수용되지 않는다.
- 검증된 이전 상태로 Git 복구가 가능하다.

## 27. 독립 Core 1차 완료 판정

### 구현 목표

독립 Agent Core Kernel이 다음 단계로 넘어갈 수 있는 완성 상태인지 종합 판정한다.

### 구현할 내용

- 1차 요구사항과 각 성공 게이트의 통합 체크리스트를 만든다.
- clean clone, Agent 진입, 자체 운영, 다중 Agent 재개, 통합 검증을 완료 조건으로 묶는다.
- 남은 실험 기능과 알려진 한계를 명시한다.
- 1차 완료에 필요하지 않은 미래 기능을 분리한다.

### 검증 방법

- 빈 환경의 clean clone에서 bootstrap부터 자체 운영 시나리오까지 실행한다.
- 각 성공 게이트의 증거와 미검증 항목을 감사한다.
- 새로운 Agent가 문서만으로 Core의 목적, 사용법, 변경 절차를 설명하게 한다.

### 성공 게이트

- 1~23단계와 26단계의 필수 성공 게이트가 모두 통과한다.
- 24·25단계의 비교 검증이 완료되고 각 Runtime의 `채택`, `실험 유지`, `제외` 처분이 근거와 함께 확정되어 있다.
- 실험 기능이 완료를 막거나 필수 경로에 숨어 있지 않다.
- 알려진 한계와 다음 단계 진입 조건이 명시되어 있다.

## 28. Host 연결 단계 진입 조건 정의

### 구현 목표

독립 Core가 준비되기 전에 Host 배포 문제로 범위가 확장되는 것을 막고 다음 단계의 시작 조건만 확정한다.

### 구현할 내용

- Host가 사용할 Core의 안정된 공개 계약과 버전을 정의한다.
- Host가 별도로 소유해야 할 정책, 상태, 데이터와 도메인 정보를 구분한다.
- Agent 지침 파일, 배포 artifact, 업데이트·롤백 문제를 다음 단계의 결정 항목으로 등록한다.
- 첫 번째와 두 번째 실제 Host 검증에 필요한 선행 조건을 정한다.

### 검증 방법

- 독립 Core 1차 완료 증거와 공개 인터페이스의 안정성을 확인한다.
- Host 정보 없이도 Core Kernel 검증이 통과하는지 재확인한다.
- Host 단계의 질문이 Core 내부 미완성 문제를 숨기고 있지 않은지 점검한다.

### 성공 게이트

- 독립 Core 1차 완료 판정이 먼저 통과했다.
- Host가 사용할 공개 계약과 Core가 알지 말아야 할 정보가 구분되어 있다.
- Host 연결은 별도 설계·승인 작업으로 시작할 준비가 되어 있다.
