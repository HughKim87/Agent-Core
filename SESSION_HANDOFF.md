# 세션 핸드오프

- 목적: 현재 단계, 활성 설계, 차단, 승인 상태와 첫 다음 행동의 단일 정본.
- 읽는 시점: 세션을 시작할 때, 작업을 이어받을 때.
- 책임: 프로젝트 에이전트가 단계 전환 시 갱신한다.
- 상태: 활성 정본.
- 관련 권위: [Core 헌장](docs/CHARTER.md)이 목적과 보장을, [완료 판정](docs/COMPLETION.md)이 독립 Core의 현재 완료 조건을 소유한다.

완료된 단계의 상세는 이 문서에 남기지 않는다. Git 커밋에서 조회한다.

---

## 현재 단계

- 단계: Q1 — 운영 정본과 라우팅 정밀도 마감 완료
- Phase: 독립 Core 1차 완료
- 활성 전체 설계: 없음. 현재 사용자 요청이 활성 소유자다.
- 활성 단계 설계: 없음. Q1 종료
- 제외 범위: Host 연결·실증, 새 Runtime·기능, Git stage·commit·push

## 직전 게이트

- submodule 소비 기준 역사 문서 정리 후 회귀 테스트 90개, 무결성 검사 14종과 통합 게이트: `pass`

이 문서는 단계 수와 무관하게 일정한 크기를 유지해야 한다. 통과 이력을 여기에 누적하면 시작 문맥이 진행에 비례해 커진다.

## 승인 상태

- 사용자가 submodule 소비에 불필요한 역사 문서 2개의 정본 흡수와 삭제를 승인했다.
- 사용자가 현재 정리 변경의 Git stage·commit을 승인했다. Git push는 승인되지 않았다.
- 참고 구현은 읽기 전용이다. 변경이 필요하면 중단하고 보고한다.
- 재개 방식: `portable`. 현재 상태는 추적된 파일과 Git 이력만으로 복원할 수 있다.

## 차단

- 내부 품질 마감 차단은 없다.
- Host 연결과 배포는 이번 범위 밖이며 별도 요청 전에는 시작하지 않는다.

## 알려진 위험

| 항목 | 내용 | 해소 시점 |
|---|---|---|
| 의미 정합성의 사람 감사 | 정본 복제·동적 수치의 의미 일부는 자동 탐지보다 활성 문서 대조에 의존한다 | 변경 시 감사 |
| Host 미검증 | Core 완료 후 별도 작업이며 현재 개선 범위에는 포함하지 않는다 | 후속 작업 |

## 첫 다음 행동

1. 새 사용자 요청을 `PROJECT_RULES.md`의 라우팅 표로 분류한다.
2. Git push는 사용자가 별도로 지시할 때만 수행한다.
3. Host 연결은 별도 승인 전에는 시작하지 않는다.
