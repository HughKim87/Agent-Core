# Agent Core 상시 정책

- 목적: 모든 소비 저장소에 적용되는 최소 안전 정책과 Core 규칙 선택 경로를 소유한다.
- 읽는 시점: 소비 저장소의 루트 진입 포인터가 지시할 때, Core 변경·검증 작업을 시작할 때.
- 책임: 사용자가 정책 변경을 승인하고 Core Maintainer가 승인된 정본을 유지한다.
- 상태: 활성 정본. Core L1 정책 계층.
- 관련 권위: 최신 사용자 지시 > 이 문서 > 소비 저장소 정책 > 현재 행동에 route된 소유자.

이 문서는 Core 공통 정책과 Core 규칙 route만 소유한다. 소비 저장소의 현재 상태, 도메인 정책, 보호 경로는 소비 저장소가 소유한다.

---

## 1. 시작

구성된 소비 저장소의 새 세션은 다음 순서로 시작한다.

1. 이 Core 정책을 끝까지 읽는다.
2. 소비 계약이 선언한 소비 저장소 정책을 끝까지 읽는다.
3. 소비 계약이 선언한 현재 상태를 끝까지 읽는다.
4. 현재 행동에 일치한 Core 규칙과 소비 저장소 규칙만 선택한다.

Core 저장소 자체에는 자동 진입 파일과 현재 상태를 두지 않는다. Core 개발은 `maintainer` 역할의 소비 저장소에서 시작한다. Core 자체 검증은 소비 상태 없이 실행할 수 있다.

## 2. 권위와 충돌

1. 최신 사용자 지시
2. 이 Core 공통 정책
3. 소비 저장소 정책
4. 현재 행동에 route된 Core·소비 저장소 소유자

소비 저장소 정책은 Core의 승인·보호·검증 경계를 완화하지 않고 프로젝트별 제한과 route만 추가한다. 같은 계층의 활성 소유자가 충돌하면 추측하지 않고 충돌한 소유자를 보고한다.

## 3. 작업 등급

충분한 것 중 가장 낮은 등급을 고른다.

| 등급 | 조건 |
|---|---|
| `quick` | 되돌릴 수 있는 국소 작업. 정책·보호 데이터·외부 효과·파괴·범위 변경이 없다 |
| `standard` | 여러 파일이나 동작을 바꾸지만 국소적이고 되돌릴 수 있으며 기존 정책 안에 있다 |
| `controlled` | 정책·구조 변경, 규칙 변경, 보호 데이터, 삭제·이동, 외부 효과, 복구 비용이 큰 작업, 사용자가 게이트를 요구한 작업 |

`controlled`는 명시적 설계와 변경 영향에 맞는 검증 게이트를 사용한다. `quick`과 `standard`에는 사용자가 요구하지 않는 계획·점수·보고서를 추가하지 않는다.

## 4. Core 규칙 라우팅

논리적 작업 하나당 아래 표에서 일치하는 소유자를 한 번씩 끝까지 읽는다. 소비 저장소의 도메인 route는 소비 정책이 별도로 소유한다.

<!-- core-rule-routes:v1 -->
| 행동 | 읽을 소유자 |
|---|---|
| 여러 파일을 바꾸거나 둘 이상의 변경·쓰기 행동을 함께 수행하는 작업을 시작하거나, 범위가 모호하거나, 사용자가 진행 중인 작업을 교정 | [작업 계약](rules/work-contract.md) |
| 의존 관계가 있는 여러 단계의 `controlled` 작업을 설계·실행·재개·전환 | [단계 작업 설계](rules/staged-work-design.md) |
| 작업을 중단·인계하거나 소비 저장소의 현재 상태를 갱신 | [현재 상태와 인수인계](rules/handoff.md) |
| 일반 유지 문서를 만들거나 바꾸거나, 보고서·계획 문서를 요청받거나, 임시 문서를 처분. 단 활성 규칙·현재 상태·실패 예방·파일 추출의 전용 소유권 판정은 제외 | [문서 작업](rules/document-work.md) |
| 파일·저장소에서 재사용 지식·현재 상태·근거·source lineage를 추출하거나 병합·정리 전에 고유 내용을 판정. 단 활성 규칙 자체의 추가·통합과 일반 요약은 제외 | [파일 지식 추출과 출처 보존](rules/file-extraction.md) |
| 파일·문서·보고서·cache·파생물의 물리적 유지·삭제·이동·개명·활성 트리 제외를 판정. 단 규칙 내용의 의미 통합은 제외 | [추출 후 파일 정리](rules/file-cleanup.md) |
| 보호 경로의 항목을 다루거나, 비밀이 있을 수 있는 파일을 다루거나, 외부 효과를 일으키기 전 | [보호 데이터와 외부 효과](rules/protected-data.md) |
| 소비 계약의 `core_path`를 생성·수정·삭제·이동·개명·재생성하거나 간접적으로 변경 | [Core 변경 통제](rules/core-change-control.md) |
| Core↔Consumer 링크·route·schema·import·저장 경로·submodule 계약·경계 테스트나 새 외부 도구 경로를 변경·감사 | [Core·Consumer 경계와 의존성](rules/boundary-routing-and-dependency.md) |
| Git 스테이지·커밋·브랜치·push·복구·백업 | [버전 관리](rules/version-control.md) |
| 다른 Agent·보고서·분석의 결론을 교차검증·비교·통합하거나 독립 재현을 주장 | [교차검증](rules/cross-validation.md) |
| 실질적이고 일반화 가능한 실패를 규칙·검사에 흡수하거나, 현재 세션에서 같은 작업의 실패를 누적·중단 | [실패 경험의 예방책 흡수](rules/failure-records.md) |
| 활성 규칙 파일을 추가·변경하거나, 추출한 내용을 공통 규칙으로 흡수·통합하거나, 규칙을 감사하거나 `controlled` 작업을 종료 | [규칙 거버넌스](rules/rule-governance.md) |
<!-- /core-rule-routes:v1 -->

라우팅은 최초 요청이 아니라 다음 실질 행동에 대해 다시 평가한다. 이미 읽은 소유자는 같은 논리적 작업에서 다시 읽지 않는다.

## 5. 결과와 근거

- 모든 사용자 대상 응답은 검증된 사실에 근거한다.
- 직접 측정, 출처 있는 사실, 추론, 가정, 미검증 주장을 구분한다.
- 실행하지 않은 검사는 `not_run`이며 통과가 아니다.
- 합성 fixture 통과를 실제 프로젝트 사용 검증이라고 하지 않는다.
- 요청된 결과만 향해 작업하고 범위를 임의로 넓히지 않는다.

## 6. 승인 경계

사용자가 소유하는 것: 목표, 금지, 보호 데이터 접근, 외부·비용 발생 행동, 되돌릴 수 없는 선택, Core 변경 범위, 결과 확인.

에이전트가 소유하는 것: 범위 안의 조사, 안전한 기본값, 되돌릴 수 있는 구현, 비례하는 검증, 실패 복구.

| 행동 | 판정 |
|---|---|
| 읽기, 분석, 검색 | 승인 불필요 |
| 새 파일 생성, 되돌릴 수 있는 승인 범위 안의 수정 | 승인 불필요 |
| 파일 삭제·이동·이름 변경 | 정확한 대상이 승인된 경우에만 |
| Git stage·commit | 사용자가 지시하거나 위임한 경우에만 |
| push, 태그, 원격 브랜치 조작 | 항상 별도 승인 필요 |
| 설치, 의존성 추가, 환경·권한·자격 증명 변경 | 항상 승인 필요 |
| 외부 쓰기·게시·업로드·전송 또는 비용 발생 | 항상 승인 필요 |
| 비밀·토큰·쿠키·개인 키 읽기 | 금지 |

프롬프트는 권한 경계가 아니다. 파일·셸·네트워크·push 권한은 실행기, 샌드박스, GitHub Deploy Key가 강제한다.

## 7. 보호 데이터

- 보호 경로는 소비 계약의 `protected_paths`가 선언한다.
- 정확한 대상과 목적 승인 없이 보호 경로를 열거·읽기·색인·링크·커밋하지 않는다.
- 검사기는 보호 경로의 상대경로 안전성만 판정하며 존재 여부 확인이나 내용 읽기를 하지 않는다.
- 비밀·자격 증명·토큰·쿠키·개인 키를 문서·로그·fixture·커밋으로 복제하지 않는다.

## 8. Core 불변

- 소비 계약의 `core_path` 전체가 Core다.
- `host` 역할에서는 Core 읽기와 승인된 공개 인터페이스 실행만 허용하고 Core 수정은 Maintainer로 이관한다.
- `maintainer` 역할에서도 Core 변경은 정확한 이유·대상·현재 사용자 승인이 모두 있어야 한다.
- 승인 후 최소 범위만 바꾸고 변경 영향에 맞는 게이트를 실행한다.

## 9. 검증과 완료

- 작업을 시작할 때 요청 결과, 변경 표면, 실패 비용에 맞춰 완료를 증명할 가장 작은 검증 집합을 선택한다.
- 필수 게이트는 사용자가 요구했거나 실제 변경 영향에 따라 선택된 검사만 뜻한다. 선택되지 않은 검사를 실행하지 않았다는 이유로 실패 처리하지 않는다.
- Agent는 결과가 불확실하거나 새 위험 신호가 발견되면 검증 방법을 추가할 수 있다. 추가 검증이 요청 범위·소요 시간·산출물을 실질적으로 확대하면 실행 전에 이유와 범위를 사용자에게 보고한다.
- 전체 회귀, 소비 통합, commit snapshot clean clone은 각각 전체 동작 영향, 소비 계약 영향, 릴리스·재현성 확인이 필요한 경우에 선택한다. 단순 변경의 기본 절차가 아니다.
- 선택된 필수 게이트 중 하나라도 `pass`가 아니면 그 게이트가 증명하려는 완료 수준에 도달하지 못한 것이다.
- Core 자체 검증과 소비 저장소 통합 검증을 구분한다.
- 도구·API의 성공 응답과 중간 상태는 실제로 관측한 경계까지만 이름 붙이고 보고한다. 요청 접수·큐잉·UI 종료·API resolve를 실제 전송·게시·수신·사용자 행동·사업 성과로 확대하지 않으며, 요청 결과에 직접 관련된 실제 파일·Git·외부 상태와 부작용을 확인한다.
- 완료 보고에는 실행한 검사, 완료 주장에 필요하지만 실행하지 못한 검사, 알려진 한계를 포함한다. 관련 없는 전체 검사 목록은 만들지 않는다.

## 10. 중단과 복구

- 권한이 없으면 변경 전에 멈추고 부분 변경을 남기지 않는다.
- 하나의 사용자 결과를 달성하기 위한 전체 실행 연쇄를 같은 작업으로 본다. 방법·도구·진단·검증·복구·하위 단계가 달라도 같은 작업의 실패는 현재 세션에서 누적한다.
- 요구 미충족에 대한 사용자 교정은 실패로 계산하고, 사용자가 실제 요구를 변경해 기존 작업 계약이 무효화된 경우는 실패로 계산하지 않는다.
- 같은 작업의 세 번째 실패에서 해당 작업 실행을 중단한다. 실패 카운터는 새 세션이나 핸드오프에 승계하지 않고, 중단을 위한 핸드오프 작성은 원 작업의 추가 실패가 아니다.
- 복구 지점은 검증된 Git commit이 소유하며 저장소 안에 백업 사본을 만들지 않는다.
